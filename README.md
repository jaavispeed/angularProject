# 🚀 Frontend – Proyecto Angular

Frontend desarrollado con **Angular** para la gestión de la aplicación.  
Este proyecto consume una API REST desarrollada en .NET y está construido utilizando la sintaxis moderna de Angular (standalone components, signals, etc.).

---

## 📋 Requisitos previos

Antes de comenzar, asegúrate de tener instalado:

- **Node.js** (versión LTS recomendada)
- **npm** (incluido con Node.js)
- **Angular CLI** (opcional, pero recomendado)

```bash
npm install -g @angular/cli
```
